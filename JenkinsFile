pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'mvn --version'
            }
        }
        stage('run tests') {
            steps {
                sh 'ls'
            }
        }
       stage('publish test results') {
            steps {
                sh 'ls'
            }
        }
        stage('deploy war files') {
            steps {
                sh 'ls'
            }
        }
    }
}
